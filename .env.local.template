# Local deployment configuration. Copy to .env.local (gitignored).
# `bun run setup` generates this file for you, including provisioning the
# KV namespaces. The same values go in GitHub Actions secrets for CI deploys.

CLOUDFLARE_ACCOUNT_ID=REPLACE_WITH_YOUR_ACCOUNT_ID
KV_NAMESPACE_ID=REPLACE_WITH_YOUR_KV_NAMESPACE_ID
KV_NAMESPACE_PREVIEW_ID=REPLACE_WITH_YOUR_PREVIEW_KV_NAMESPACE_ID
